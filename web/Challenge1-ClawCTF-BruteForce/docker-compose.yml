version: '3.8'

services:
  clawctf-challenge:
    build: .
    container_name: clawctf-brute-force
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - PORT=5000
    restart: unless-stopped
