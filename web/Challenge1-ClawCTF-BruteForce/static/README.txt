Placeholder images for the ClawCTF challenge.

Note: The application references:
- clawctflogo.png (used in the left panel header)
- ClawCTF.png (used as favicon)

You can add your own logo images here or the app will use external URLs from the HTML.
The app will still work without these local images since it uses external image URLs as fallback.
